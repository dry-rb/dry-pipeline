require 'spec_helper'

describe Dry::Pipeline do
  it 'has a version number' do
    expect(Dry::Pipeline::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
